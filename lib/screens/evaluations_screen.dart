
import 'package:flutter/material.dart';

class EvaluationsScreen extends StatelessWidget {
  const EvaluationsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const  Center(
      child: Text('Evaluations Screen'),
    );
  }
}
